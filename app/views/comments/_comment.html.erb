<div id="<%= dom_id(comment) %>">
  <%= comment.content %> - <%= time_tag comment.created_at, 'data-local': 'time-ago' %>
  <%= button_to "Destroy this comment", post_comment_path(comment.post, comment), method: :delete, class: 'btn btn-link' %>
</div>